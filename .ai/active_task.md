# 当前任务：RBAC 角色删除（带安全护栏）

## 当前目标
允许管理员删除角色，但遵循最佳实践：
- 禁止删除系统角色（admin/user）
- 默认禁止删除仍被用户引用的角色（要求先解绑）
- 前端提供删除按钮与二次确认

## 实施进度
- ⏳ 进行中：后端 delete role 增加保护（system role / in-use）
- ⏳ 进行中：前端 `/admin/rbac` 增加删除入口（仅非系统角色、rbac:write）

## 下一步（短期）
1. 实现并测试后端 delete role 保护逻辑。
2. 增加前端删除按钮与确认弹窗，删除后刷新列表并清理选中态。
3. 通过 `pnpm lint-staged`。
